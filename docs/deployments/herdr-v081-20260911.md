# Herdr / Bohe Zylos 0.8.1 source

Based on the CL0.8.1 deployment overlay, with the exact additional installed
`cli/lib/runtime/codex.js` session-name override preserved. Bohe uses the stable
`claude-main` tmux identity through `ZYLOS_TMUX_SESSION`; default remains
`codex-main`. This is an upstream single-session deployment, not the devbox fork.

Both observed package patches are unchanged from the verified0.7.1 installation.
Keep all runtime skill patches, provider sidecars, custom Feishu fork, Telegram,
profiles and state. Live global package is root-owned `/usr/lib/node_modules/zylos`
under Node22; the user's npm prefix points elsewhere and must not select the
installation destination. Suppress lifecycle hooks and update only the Web
Console dependency manifests/lock and dependency tree in the role runtime.

The inherited base passed151 Jest and929 Node tests. The session override was
verified directly with `ZYLOS_TMUX_SESSION=claude-main`; runtime launch tests pass.
Live installation/rollback evidence is tracked in azure-agents-infra AX-366.
